[DEFAULT]
# This section is used for the program itself right now so do not edit this section
supported_db_types = ['postgresql', 'mysql']

# the section header is really an alias for idinifing your databases
[example-db]
# if you want to enable the backup for this database
enabled = True
# the type of database
db_type = postgresql
# connection paramiters for the DB
db_host = host_of_database
db_name = user
db_user = postgres
db_pass = password123
db_port = 5432

# these are passed into a requests.put method (futures will improve this section)
upload_url = https://backups.example.com/backups/
upload_user = backupuser
upload_pass = password543